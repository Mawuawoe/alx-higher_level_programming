#!/usr/bin/node

const toPrint = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
const toPrintLength = toPrint.length;

for (let i = 0; i < toPrintLength; i++) {
  console.log(toPrint[i]);
}
